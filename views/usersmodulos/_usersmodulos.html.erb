<div align="center"><table width="100%" border="0" cellpadding="2" cellspacing="2" align="center">
  <tr>
    <td height="30"><div class="col_enc">ASOCIACION DE MODULOS</div></td>
  </tr>
  <tr>
    <td height="10"></td>
  </tr>
</table>
  </div>
<% remote_form_for [@user, @usersmodulo] do |form| %>
<% if !@usersmodulo.id %>
	<%= render :partial => "/usersmodulos/usersmodulos_form" %>
<% end %>
      <div align="center">
<table width="60%" border="0" align="center" cellpadding="0" cellspacing="0">
  <tr valign="middle">
    <td width="85%" height="30" class="oce-first">Modulo</td>
    <td width="15%" height="30" class="oce-first"></td>
  </tr>
</table>
<% for usersmodulo in @user.usersmodulos.find(:all, :order => 'modulo_id') %>
   <% if usersmodulo.id != @usersmodulo.id %>
  <table width="60%" border="0" align="center" cellpadding="0" cellspacing="0">
    <tr  class="<%=cycle('even1', 'odd1')%>" id="usersmodulo_<%=usersmodulo.id%>">
     <td width="85%" class="camposDesc"><%=usersmodulo.modulo.descripcion%></td>
     <td width="15%" class="camposDesc">
      <%= link_to_remote('Borrar',{:url => user_usersmodulo_path(@user, usersmodulo),
		:method => :delete, :confirm => "¿Seguro?"}, :class=>"borrarlink")%>
</td>
 </tr>
</table>
   <% end %>
<% end %>
            <%= hidden_field :user, :id %>

</div>     
<% end %>

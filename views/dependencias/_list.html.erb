  <div align="center">
<table width="50%" border="0" cellpadding="0" cellspacing="0" id="one-column-emphasis">
  <tr>
    <td class="oce-first" width="70%">Descripcion</td>
    <td class="oce-first" width="30%"></td>
  </tr>
<% for dependencia in @dependencias%>
  <tr postS_<%=dependencia.id%>>
    <td class="camposDesc"><%=h dependencia.descripcion %></td>
    <td class="camposDesc"><div align="center">
        <% if permiso("dependencia","A").to_s == "S" %>
        <%= link_to 'Editar', edit_dependencia_path(dependencia), :class=>"editarlink" %>
        <% end %>
        <%= link_to 'Reenvio', dependencia_dependenciasusers_path(dependencia), :class=>"imagenpagos" %>
        </div>
   </td>
  </tr>
<% end %>
</table>
</div>

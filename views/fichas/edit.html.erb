<h1>Editing ficha</h1>

<% form_for(@ficha) do |f| %>
  <%= f.error_messages %>

  
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @ficha %> |
<%= link_to 'Back', fichas_path %>